/*Миксин для компонента фильтрации.*/

/*import Filter from 'src/components/COMMON/Filter'

export const search = {
    components: {
        Filter
    },

    data() {
        return {
            filterOpt: ''
        }
    },

    methods: {
        setFilterOpt(str = '') {
            this.filterOpt = str
        }
    }
} */